fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pdfplumber==0.11.0
python-docx==1.1.2
pydantic==2.7.4

# ML stack for perplexity scoring. CPU-only torch keeps the image smaller;
# swap for the CUDA build if you deploy on a GPU host.
torch==2.3.1
transformers==4.42.3
